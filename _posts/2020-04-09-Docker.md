---
layout: mypost
title: 寒假手记
categories: [置顶]

---

Update: 2020/04/09

## 安装

```
sudo apt-get remove docker docker-engine docker.io containerd runc # 卸载旧版本

sudo apt-get update # 更新 apt 包索引

sudo apt-get install \
    apt-transport-https \
    ca-certificates \
    curl \
    gnupg-agent \
    software-properties-common # 安装 apt 依赖包，用于通过HTTPS来获取仓库

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add - # 添加 Docker 的官方 GPG 密钥

sudo apt-key fingerprint 0EBFCD88 # 通过搜索指纹的后8个字符，验证您现在是否拥有带有指纹的密钥。

sudo add-apt-repository \
   "deb [arch=amd64] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) \
  stable" # 设置稳定版仓库

sudo apt-get install docker-ce docker-ce-cli containerd.io # 安装最新版本的 Docker Engine-Community 和 containerd ，或者转到下一步安装特定版本

apt-cache madison docker-ce # 查找可用版本
```

版本格式为诸如5:18.09.1\~3-0\~ubuntu-xenial, 替换到<·>中：

```
sudo apt-get install docker-ce=<VERSION_STRING> docker-ce-cli=<VERSION_STRING> containerd.io
```

检验是否安装成功：

```
sudo docker run hello-world
```

启动docker：

```
sudo systemctl enable docker
sudo systemctl start docker
```

## Jupiter 配置文件

位置：/root/.jupyter/jupyter_notebook_config.py

我放这里了：Programs/config/.jupyter















